# Core — depth model + webcam testbed (test.py)
# Python 3.13 / tested on macOS (MPS)
# torch>=2.12
# torchvision>=0.27
opencv-python>=4.13
numpy>=2.0

# 3D pipeline (calibrate.py, view3d.py)
rerun-sdk>=0.34      # 3D viewer (point clouds, boxes, timeline)
ultralytics>=8.4     # YOLO person detection

# Gradio demo only (app.py) — not needed for the tracking pipeline
gradio
gradio_imageslider
matplotlib
